{
  "name": "aod-server",
  "version": "1.0.0",
  "description": "AI Orchestration Dashboard - Backend API Gateway",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "db:init": "bun run db/init.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "ioredis": "^5.4.0",
    "nanoid": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
