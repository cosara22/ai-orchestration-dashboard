# AIエージェントチーム × Gitブランチ並列開発フロー

チーム単位でブランチを分離し、ファイル競合なく並列作業を行うアーキテクチャ

---

## 全体構成図

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              AOD Dashboard                                       │
│                     (統合監視・タスク管理・マージ調整)                           │
└───────────────────────────────────┬─────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│    Team Alpha     │   │    Team Beta      │   │    Team Gamma     │
│  (Feature A開発)  │   │  (Feature B開発)  │   │   (Bugfix/維持)   │
└─────────┬─────────┘   └─────────┬─────────┘   └─────────┬─────────┘
          │                       │                       │
          ▼                       ▼                       ▼
┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐
│ branch: feature/A │   │ branch: feature/B │   │ branch: hotfix/X  │
│                   │   │                   │   │                   │
│ ┌───────────────┐ │   │ ┌───────────────┐ │   │ ┌───────────────┐ │
│ │ worktree/     │ │   │ │ worktree/     │ │   │ │ worktree/     │ │
│ │ feature-A/    │ │   │ │ feature-B/    │ │   │ │ hotfix-X/     │ │
│ └───────────────┘ │   │ └───────────────┘ │   │ └───────────────┘ │
└───────────────────┘   └───────────────────┘   └───────────────────┘
          │                       │                       │
          └───────────────────────┼───────────────────────┘
                                  │
                                  ▼
                        ┌───────────────────┐
                        │   main branch     │
                        │  (統合・リリース) │
                        └───────────────────┘
```

---

## Git Worktree を使った物理分離

```
プロジェクトルート: C:\Projects\my-app\
│
├── .git/                          ← 共有Gitオブジェクト
│
├── main/                          ← main ブランチ (参照用)
│   └── src/
│
├── worktrees/
│   ├── feature-A/                 ← Team Alpha 専用ディレクトリ
│   │   ├── src/
│   │   └── .claude/
│   │
│   ├── feature-B/                 ← Team Beta 専用ディレクトリ
│   │   ├── src/
│   │   └── .claude/
│   │
│   └── hotfix-X/                  ← Team Gamma 専用ディレクトリ
│       ├── src/
│       └── .claude/
│
└── AOD連携設定
```

### Worktree作成コマンド

```bash
# Gitワークツリーで各チーム用ディレクトリを作成
cd my-app
git worktree add worktrees/feature-A feature/auth-api
git worktree add worktrees/feature-B feature/dashboard-ui
git worktree add worktrees/hotfix-X hotfix/critical-bug
```

---

## チーム別ターミナル配置

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         Terminal Layout                                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Team Alpha (worktrees/feature-A/)                                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                 │
│  │ T1: Lead        │  │ T2: Backend     │  │ T3: Test        │                 │
│  │ cwd: feature-A/ │  │ cwd: feature-A/ │  │ cwd: feature-A/ │                 │
│  │ branch: feat/A  │  │ branch: feat/A  │  │ branch: feat/A  │                 │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                 │
│       同一ブランチ内 → ファイルロックで競合防止                                 │
│                                                                                  │
│  ──────────────────────────────────────────────────────────────────────────────  │
│                                                                                  │
│  Team Beta (worktrees/feature-B/)                                               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                 │
│  │ T4: Lead        │  │ T5: Frontend    │  │ T6: Design      │                 │
│  │ cwd: feature-B/ │  │ cwd: feature-B/ │  │ cwd: feature-B/ │                 │
│  │ branch: feat/B  │  │ branch: feat/B  │  │ branch: feat/B  │                 │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                 │
│       別ブランチ → Team Alpha とは完全に独立（競合なし）                        │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 並列作業フロー

```
時間軸 →

Team Alpha (feat/A)     Team Beta (feat/B)     main branch
─────────────────────   ─────────────────────   ─────────────────────
     │                        │                       │
     │ ← T1,T2,T3 並列作業    │ ← T4,T5,T6 並列作業  │
     │    (ファイルロック)    │    (ファイルロック)   │
     │                        │                       │
     ├── src/auth/ 編集       ├── src/ui/ 編集        │
     ├── src/api/ 編集        ├── src/components/     │
     │                        │                       │
     │                        │                       │
     ▼                        │                       │
  PR作成 ─────────────────────│───────────────────────▶ Review
     │                        │                       │
     │                        ▼                       │
     │                     PR作成 ──────────────────▶ Review
     │                        │                       │
     ▼                        │                       │
  Merge ──────────────────────│───────────────────────▶ main
     │                        │                       │ ← feat/A統合
     │                        ▼                       │
     │                     Merge ────────────────────▶ main
     │                        │                       │ ← feat/B統合
     │                        │                       │
   完了                      完了                    リリース準備
```

---

## 競合防止の多層防御

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         3層の競合防止                                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Layer 1: ブランチ分離（チーム間）                                              │
│  ────────────────────────────────                                               │
│  Team Alpha → feature/A ブランチ                                                │
│  Team Beta  → feature/B ブランチ                                                │
│  → 異なるブランチなので物理的に競合しない                                       │
│                                                                                  │
│  Layer 2: ディレクトリ分離（worktree）                                          │
│  ────────────────────────────────────                                           │
│  worktrees/feature-A/ ← Team Alpha 専用                                         │
│  worktrees/feature-B/ ← Team Beta 専用                                          │
│  → 同時編集しても別ファイルなので安全                                           │
│                                                                                  │
│  Layer 3: ファイルロック（チーム内）                                            │
│  ────────────────────────────────                                               │
│  同一チーム内で同じファイルを編集する場合:                                      │
│  POST /api/locks/acquire { file: "src/auth.ts", agent: "T2" }                   │
│  → AODのロック機能で排他制御                                                    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## AODでの管理イメージ

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│ AOD Dashboard - Branch Overview                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│ Branches                                                                         │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ feature/A (Team Alpha)                    Progress: ████████░░ 78%         │ │
│ │ ├─ T1: Reviewing PR #42                                                    │ │
│ │ ├─ T2: Working on src/auth/jwt.ts 🔒                                       │ │
│ │ └─ T3: Running tests                                                       │ │
│ │ Last commit: 5 min ago | Files changed: 12 | +342 -89                      │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ feature/B (Team Beta)                     Progress: ██████░░░░ 56%         │ │
│ │ ├─ T4: Designing component structure                                       │ │
│ │ ├─ T5: Working on src/components/Header.tsx 🔒                             │ │
│ │ └─ T6: Creating design tokens                                              │ │
│ │ Last commit: 12 min ago | Files changed: 8 | +189 -23                      │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ main                                      Ready for release ✓              │ │
│ │ Pending PRs: 2 (feat/A, feat/B)                                            │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## まとめ

| 層 | 対象 | 方法 |
|----|------|------|
| **チーム間** | 物理分離 | Git worktree + 別ブランチ |
| **チーム内** | 論理分離 | AODファイルロック |
| **統合時** | マージ管理 | PR + コードレビュー |
